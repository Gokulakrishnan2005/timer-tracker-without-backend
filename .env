# React Native App Configuration

# API Configuration
# The app will automatically detect the best IP address for your network
# You can also manually set the IP if auto-detection doesn't work

# Backend API URL (leave empty for auto-detection)
# REACT_NATIVE_API_URL=http://192.168.1.100:5000/api

# Environment
NODE_ENV=development

# Optional: Custom timeout for API requests (in milliseconds)
API_TIMEOUT=10000

# Optional: Enable debug logging
DEBUG=true
